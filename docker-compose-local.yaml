services:
  spring-app:
    container_name: spring-app
    build:
      context: .
      dockerfile: Dockerfile-local
    ports:
      - "8080:8080"
    environment:
      SPRING_PROFILES_ACTIVE: local
    networks:
      - app-network

networks:
  app-network:
    driver: bridge